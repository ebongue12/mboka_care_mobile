name: mboka_care_mobile
description: Application MBOKA-CARE
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.9
  shared_preferences: ^2.2.2
  dio: ^5.4.0
  qr_flutter: ^4.1.0
  intl: ^0.18.1
  google_fonts: ^6.1.0
  image_picker: ^1.0.5
  file_picker: ^6.1.1
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/
