import 'package:flutter/material.dart';
class HealthPriorityScreen extends StatelessWidget {
  const HealthPriorityScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(title: const Text('Priorité Santé')),
    body: const Center(child: Text('Santé - À implémenter')));
}
